#To host the PHP server AND database, execute this line in a seperate terminal window:

`docker compose up -d --build`

To run the front-end execute:

`npm run build && npm run dev`

The website will be on:

`localhost:5173`
